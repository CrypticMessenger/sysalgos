package main

func main() {
	// var jobs []Job         //stores all jobs
	// var i_o_queue [][2]int // stores (job_id,time_to_end_io) in form queue

	//take input number of jobs
	// in a loop, take input for each job, and set default values for other fields
	// append each job to jobs
	// create queues, each initialized with empty jobs
	// initialize CurrentTime (globalized) = 0
	// initiialize finished_jobs = 0
	// while (finished_jobs < len(jobs)) {
	// 	// check if any job has arrived at CurrentTime
	// 	// 	//if yes, add it to the highest priority queue
	// 	// check if any job has completed its I/O at CurrentTime
	// 	// 	//if yes, add it to it's queue, also set doingIo to false
	// 	// check if it's time to boost
	// 	// 	//if yes, move all jobs to highest priority queue and reset allotedTimeLeft, ticksLeft
	// 	// run the highest priority queue by reducing allotedTimeLeft and ticksLeft for job not doing I/O
	// 	// 	//if time to do I/O, move the job to I/O queue and set doingIo to true
	// 	// 	//if allotedTimeLeft == 0, move the job to next lower priority queue and reset allotedTimeLeft, ticksLeft
	// 	// 	//if ticksLeft == 0, move the job to the end of the same queue
	// 	// 	//if job is finished, set endTime and increment finished_jobs
	// 	// increment CurrentTime
	// }

}
